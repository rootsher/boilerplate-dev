server {
    listen      80;
    server_name project.localhost www.project.localhost;
    access_log  /var/log/project.access.log  main;

    location / {
        proxy_pass  http://frontend:${PORT_FRONTEND};
    }
}
