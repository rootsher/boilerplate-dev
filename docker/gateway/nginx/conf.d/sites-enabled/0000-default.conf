server {
    listen 80 default;

    location / {
        default_type text/html;
        return 200 "<body style='margin: 0'><div style='text-align:center; position: absolute; top: 50%; width: 100%; font-size: 1.5em;'>You got yourself to the default server.</div></body>";
    }
}
